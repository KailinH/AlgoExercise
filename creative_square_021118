#calculate the square of a number without ^ or **
#idea: addition formula for the equal difference sequence
def square(n):
"""
n: the number whose square we are looking for
"""
    res = 0
    if n <= 0:
        return 'input error'
    for i in range(1,n+1):
        res += (2*i - 1)
    return res          
